<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio Prototype</title>
    <style>
        /* 基本設定 */
        body {
            margin: 0;
            padding: 0;
            font-family: "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN", sans-serif;
            background-color: #111; /* 深い黒 */
            color: #fff;
            overflow-x: hidden; /* 横スクロール防止 */
        }

        /* 共通：斜めセクションの魔法
           親を傾けて(skewY)、子は逆方向に傾けて戻すのがコツです */
        .angled-section {
            position: relative;
            padding: 100px 0;
            transform: skewY(-3deg); /* 全体を斜めにする */
            transform-origin: top left;
        }
        
        .content-fix {
            transform: skewY(3deg); /* 中身だけ逆回転して水平に戻す */
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
        }

        /* ヒーローセクション（トップ） */
        .hero {
            background-color: #D32F2F; /* ペルソナ風の赤 */
            margin-top: -50px; /* 上部の隙間埋め */
            padding-top: 150px;
            padding-bottom: 150px;
        }

        h1 {
            font-size: 4rem;
            font-weight: 900;
            line-height: 1.1;
            margin: 0;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        p.subtitle {
            font-size: 1.5rem;
            margin-top: 20px;
            opacity: 0.9;
        }

        /* 作品紹介セクション */
        .projects {
            background-color: #1a1a1a; /* ダークグレー */
            z-index: 1;
        }

        .project-card {
            background: #fff;
            color: #000;
            padding: 40px;
            margin-top: 50px;
            border-radius: 4px; /* 角を少しだけ丸くして現代的に */
            box-shadow: 0 20px 40px rgba(0,0,0,0.5);
            
            /* グリッドレイアウトで画像と文字を横並び */
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            align-items: center;
        }

        .project-image {
            background-color: #ccc;
            height: 300px;
            width: 100%;
            object-fit: cover;
        }

        h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
        }
        
        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 15px 30px;
            background: #000;
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s;
        }
        .btn:hover {
            background: #D32F2F;
        }

        /* 3つ目のセクション（白背景でメリハリ） */
        .about {
            background-color: #f4f4f4;
            color: #111;
        }

    </style>
</head>
<body>

    <section class="angled-section hero">
        <div class="content-fix">
            <h1>Shape<br>Puzzles</h1>
            <p class="subtitle">Designing Logic & Emotion.</p>
        </div>
    </section>

    <section class="angled-section projects">
        <div class="content-fix">
            <div class="project-card">
                <div class="project-info">
                    <h2>01. Project Name</h2>
                    <p>ここにプロジェクトの説明が入ります。背景は斜めですが、このカード自体は真っ直ぐなので非常に読みやすいはずです。ペルソナのような配色は維持しつつ、情報のノイズを減らしました。</p>
                    <a href="#" class="btn">View Case Study</a>
                </div>
                <div class="project-image" style="background:url('https://placehold.co/600x400/222/fff') center/cover;"></div>
            </div>
        </div>
    </section>

    <section class="angled-section about">
        <div class="content-fix">
            <h2>About Me</h2>
            <p>ここに自己紹介が入ります。セクションごとに背景色が切り替わる際、斜めのラインが「鋭さ」を演出します。</p>
        </div>
    </section>

</body>
</html>
